import { Metadata } from 'next';

export const metadata: Metadata = {
    title: {
        absolute: 'Administrative page'
    }
};

export default function DashboardPage() {
    return <div>DashboardPage</div>;
}
